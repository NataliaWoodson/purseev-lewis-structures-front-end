<Layer>
  <Group>
    {atoms.map((atom) => (
      <Group>
        {/* <Text
                x={atom.x}
                y={atom.y}
                text={atom.text}
                draggable
                fontSize={30}
              /> */}
      </Group>
    ))}
  </Group>
</Layer>;
